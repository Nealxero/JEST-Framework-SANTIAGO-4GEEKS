// Write your function here

let array = [1, 2, 3, 2, 1];
let keeper = (2);

keep = (array, keeper) => {

if (Array.isArray(array === false)) {
    return [];
}
    return array.filter(function(result) {
        return console.log(result == keeper) ;
        
    });
}


